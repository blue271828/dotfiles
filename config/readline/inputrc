$if bash
  set completion-ignore-case on
  set mark-symlinked-directories on

  # for type japanese
  set input-meta on
  set output-meta on
  set convert-meta off

  # Enable to be autocompletion with one tab
  set show-all-if-ambiguous on
$endif
